enum AttachmentsFilter { all, files, links }

enum Order {
  asc,
  desc,
}

enum ViewType {
  filter,
  list,
  form,
  newForm,
}

enum ButtonType {
  primary,
  secondary,
}

enum ConnectivityStatus {
  wiFi,
  cellular,
  offline,
}

enum ViewState {
  idle,
  busy,
}

enum ImageShape {
  roundedRectangle,
  circle,
}
